import 'package:flutter/material.dart';

class InsertMoneyPanel extends StatelessWidget {
  const InsertMoneyPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
